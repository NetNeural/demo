# Production Environment Variables for NetNeural IoT Platform
# These values will be set in GitHub Secrets for deployment

# ============================================================================
# Application Configuration
# ============================================================================
NODE_ENV=production
NEXT_PUBLIC_APP_NAME="NetNeural IoT Platform"
NEXT_PUBLIC_APP_VERSION="1.0.0"
NEXT_PUBLIC_APP_URL="https://netneural.github.io/MonoRepo"

# ============================================================================
# Supabase Configuration (Production)
# ============================================================================
# IMPORTANT: These values are managed via GitHub Secrets
# DO NOT set NEXT_PUBLIC_* variables here - they are injected by GitHub Actions
# Setting them here will override the secrets with placeholder text!
# NEXT_PUBLIC_SUPABASE_URL = set via GitHub Actions
# NEXT_PUBLIC_SUPABASE_ANON_KEY = set via GitHub Actions
# SUPABASE_SERVICE_ROLE_KEY = set via GitHub Actions
# SUPABASE_PROJECT_REF = set via GitHub Actions
# SUPABASE_DB_PASSWORD = set via GitHub Actions

# ============================================================================
# External API Integrations (Production)
# ============================================================================
# IMPORTANT: Managed via GitHub Secrets
# DO NOT set API keys here - they are injected by GitHub Actions
# GOLIOTH_API_KEY = set via GitHub Actions
GOLIOTH_PROJECT_ID=nn-cellular-alerts
GOLIOTH_BASE_URL=https://api.golioth.io

# OpenAI API Key (for AI-powered predictive insights)
# OPENAI_API_KEY = set via GitHub Actions

# Twilio SMS Configuration
# IMPORTANT: Managed via GitHub Secrets
# TWILIO_ACCOUNT_SID = set via GitHub Actions
# TWILIO_AUTH_TOKEN = set via GitHub Actions
# TWILIO_FROM_NUMBER = set via GitHub Actions (e.g., +1234567890)

# ============================================================================
# GitHub Deployment Configuration
# ============================================================================
# IMPORTANT: Managed via GitHub Secrets
# GITHUB_TOKEN = set via GitHub Actions
# SUPABASE_ACCESS_TOKEN = set via GitHub Actions

# ============================================================================
# Static Export Configuration
# ============================================================================
BUILD_MODE=static
NEXT_PUBLIC_STATIC_EXPORT=true