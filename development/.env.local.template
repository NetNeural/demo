# Environment Variables for NetNeural IoT Platform
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local. Replace any real secret values with placeholders.
# This example intentionally uses development-safe or placeholder values only.

# ============================================================================
# Application Configuration
# ============================================================================
NODE_ENV=development
NEXT_PUBLIC_APP_NAME="NetNeural IoT Platform"
NEXT_PUBLIC_APP_VERSION="1.0.0"
DEBUG=true
ANALYZE=false

# ============================================================================
# Supabase Configuration (Primary Backend)
# ============================================================================

# Local Development (use when running `supabase start`)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=local-anon-key-placeholder
SUPABASE_SERVICE_ROLE_KEY=local-service-role-key-placeholder

# Production Supabase (obtain from Supabase Dashboard > Settings > API)
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
# PROD_SUPABASE_SERVICE_ROLE_KEY=your-prod-service-role-key-here

# Supabase CLI Configuration
# SUPABASE_PROJECT_REF=your-project-ref
# SUPABASE_ACCESS_TOKEN=your-access-token-here
# SUPABASE_DB_PASSWORD=your-database-password

# ============================================================================
# External API Integrations
# ============================================================================

# Golioth IoT Platform API Key (from Golioth Console > Access Management)
GOLIOTH_API_KEY=golioth-key-placeholder
GOLIOTH_PROJECT_ID=your-golioth-project-id
GOLIOTH_BASE_URL=https://api.golioth.io

# Twilio SMS Configuration (from Twilio Console > Account > API Keys & Tokens)
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_FROM_NUMBER=+1234567890

# === External Services ===
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=testuser
SMTP_PASS=testpass

# === Security ===
JWT_SECRET=dev-jwt-secret-placeholder-32charsxxxxxxxx
ENCRYPTION_KEY=dev-encryption-key-placeholder-32chars

# === Monitoring ===
# SENTRY_DSN=your-sentry-dsn
LOG_LEVEL=debug

# === Development Configuration ===
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api
# DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# ----------------------------------------------------------------------------
# Edge Function Secrets (managed via `supabase secrets set` in production)
# ----------------------------------------------------------------------------
# STRIPE_SECRET_KEY=sk_live_placeholder
# EDGE_GOLIOTH_API_KEY=golioth-edge-placeholder

# To sync secrets to Supabase for Edge Functions:
#   supabase secrets set --env-file supabase/functions/.env
